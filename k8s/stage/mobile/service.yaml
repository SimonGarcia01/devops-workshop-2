apiVersion: v1
kind: Service
metadata:
  name: mobile-web-service
  namespace: stage
spec:
  selector:
    app: mobile-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31090
  type: NodePort
