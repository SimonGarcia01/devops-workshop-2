apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: stage
spec:
  selector:
    app: notification-service
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 31082
  type: NodePort
