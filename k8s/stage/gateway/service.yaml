apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: stage
spec:
  selector:
    app: gateway-service
  ports:
    - port: 8087
      targetPort: 8087
      nodePort: 31087
  type: NodePort
