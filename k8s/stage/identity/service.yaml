apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: stage
spec:
  selector:
    app: identity-service
  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 31083
  type: NodePort
