apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: prod
spec:
  selector:
    app: auth-service
  ports:
    - port: 8180
      targetPort: 8180
      nodePort: 32081
  type: NodePort
