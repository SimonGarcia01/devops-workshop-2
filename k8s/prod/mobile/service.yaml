apiVersion: v1
kind: Service
metadata:
  name: mobile-web-service
  namespace: prod
spec:
  selector:
    app: mobile-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 32090
  type: NodePort
