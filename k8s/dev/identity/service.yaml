apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: dev

spec:
  selector:
    app: identity-service

  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 30083

  type: NodePort
