apiVersion: v1
kind: Service
metadata:
  name: promotion-service
  namespace: dev
spec:
  selector:
    app: promotion-service

  ports:
    - protocol: TCP
      port: 8088
      targetPort: 8088

  type: ClusterIP
