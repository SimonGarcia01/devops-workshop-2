apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: dev

spec:
  selector:
    app: dashboard-service

  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
      nodePort: 30084

  type: NodePort
