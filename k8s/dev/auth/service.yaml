apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: auth-service
  ports:
    - port: 8180
      targetPort: 8180
      nodePort: 30081
