apiVersion: v1
kind: Service
metadata:
  name: mobile-web
  namespace: dev

spec:
  selector:
    app: mobile-web

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30090

  type: NodePort
